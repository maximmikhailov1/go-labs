# Copy to .env and adjust. Used for local runs and docker compose.

# Database (for docker-compose.local.yml and local backend)
POSTGRES_USER=labs
POSTGRES_PASSWORD=labs
POSTGRES_DB=labs
DB_URL=postgres://labs:labs@localhost:5432/labs?sslmode=disable

# Backend
PORT=3000
SECRET=your-jwt-secret-min-32-chars
TUTOR_SECRET=tutorsecret

# Frontend (only when running frontend locally: npm run dev)
# NEXT_PUBLIC_API_URL=http://localhost:3000
